export enum Type {
    square = 'square',
    medium = 'medium',
    small = 'small',
    xsmall = 'xsmall',
}

export enum Filter {
    mono = 'mono',
    negate = 'negate',
    custom = 'custom',
}

export enum Fit {
    cover = 'cover',
    contain = 'contain',
    fill = 'fill',
    inside = 'inside',
    outside = 'outside',
}

export enum Position {
    top = 'top',
    right_top = 'right top',
    right = 'right',
    right_bottom = 'right bottom',
    bottom = 'bottom',
    left_bottom = 'left bottom',
    left = 'left',
    left_top = 'left top',
    center = 'center',
}